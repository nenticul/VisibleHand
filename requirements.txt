# ── Web framework ──────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
slowapi>=0.1.9

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9            # PostgreSQL (optional for local dev with SQLite)
alembic>=1.13.0

# ── Ingestion & scheduling ─────────────────────────────────────────────────────
httpx>=0.27.0
apscheduler>=3.10.4
tenacity>=8.2.0

# ── Scoring ────────────────────────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.12.0                     # Hawkes process MLE (L-BFGS-B)

# ── NLP — base (always installed) ─────────────────────────────────────────────
# transformers and torch are optional; the API degrades gracefully without them.
# Install manually for full FinBERT support:
#   pip install transformers torch onnxruntime
# transformers>=4.40.0
# torch>=2.3.0
# onnxruntime>=1.18.0

pdfplumber>=0.11.0

# ── Observability (optional — install in production) ──────────────────────────
# pip install prometheus_client sentry-sdk[fastapi,sqlalchemy] structlog
# prometheus_client>=0.20.0
# sentry-sdk>=2.0.0
# structlog>=24.1.0

# ── Calibration (optional — install for weight optimization) ──────────────────
# pip install optuna
# optuna>=3.6.0

# ── Dev / testing ──────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-env>=1.1.0
ruff>=0.4.0
mypy>=1.9.0

# ── Docs (optional) ────────────────────────────────────────────────────────────
# pip install mkdocs mkdocs-material mkdocstrings[python]
