# Base URL of the VisibleHand API the site pulls live data from.
# Defaults to the public hosted instance if unset.
# Local dev against your own API: VITE_API_BASE=http://localhost:8080
VITE_API_BASE=https://api.visiblehand.xyz
